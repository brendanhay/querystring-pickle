:set -isrc
:set -XDeriveGeneric
:set -XOverloadedStrings

:load Network.HTTP.QueryString.Pickle

import           Data.ByteString       (ByteString)
import qualified Data.ByteString.Char8 as BS
import           GHC.Generics
import           System.IO

hSetBuffering stdout LineBuffering

data A = B | C deriving (Show, Generic)

instance IsQuery A

data R = R { rA :: A } deriving (Show, Generic)

instance IsQuery R

let r = R C

